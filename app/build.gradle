apply plugin: 'application'

version = "0.0.1"
sourceCompatibility = theSourceCompatibility

mainClassName = 'com.markdownedit.app.ApplicationMain'

dependencies {
  compile project(':api')

  compile group: 'org.fxmisc.richtext', name: 'richtextfx', version: '0.6.3'
}
